# Semantic rebase

Direct transplantation is attempted only for compatible schemas and earns a
claim only after target, guard, and new-base preservation checks pass. Otherwise
the old patched model teaches target behavior while the new base teaches guards.
Cross-architecture tiny rebase creates a new behavior patch and never maps
physical tensors.

