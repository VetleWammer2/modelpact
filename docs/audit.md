# Higher-order audit

Small pools can execute every subset. Larger pools fit a sparse pseudo-Boolean
surrogate to executed contract margins, actively propose uncertain or predicted
failing subsets, execute them, and update the surrogate. Any failure is reduced
with executed ddmin removal tests. Surrogate predictions are never certificates.

